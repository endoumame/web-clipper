/* oxlint-disable no-unsafe-call, no-unsafe-member-access, no-unsafe-argument -- Vue SFC types not resolved by oxlint */
import "./style.css";
import App from "./App.vue";
import { createApp } from "vue";

createApp(App).mount("#app");
